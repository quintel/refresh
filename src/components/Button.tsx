import styled from '@emotion/styled';

export interface ButtonProps {
  children: React.ReactNode;
}

export default styled.button`
  background: hsl(222deg 62% 60%);
  border: 0;
  color: #fff;
  padding: 0.5rem 0.75rem;
`;
